/* Practice Lab challenges. runner: python | js | sql | manual.
   python/js tests: {name, call, expect} — both are expressions evaluated after the learner's code.
   sql: setup (run first), expect (rows of the LAST result set), unordered (optional). */
window.CHALLENGES = [
  /* ===================== PYTHON ===================== */
  { id: 'py-reverse', cat: 'Python', diff: 'easy', title: 'Reverse a string', runner: 'python',
    prompt: 'Write `reverse_string(s)` that returns the string reversed. `reverse_string("policy")` → `"ycilop"`.',
    starter: 'def reverse_string(s):\n    # your code here\n    pass\n', hints: ['Slicing accepts a step: `s[::-1]`.'],
    tests: [{ call: 'reverse_string("policy")', expect: '"ycilop"' }, { call: 'reverse_string("")', expect: '""' }, { call: 'reverse_string("a b")', expect: '"b a"' }],
    solution: 'def reverse_string(s):\n    return s[::-1]' },
  { id: 'py-vowels', cat: 'Python', diff: 'easy', title: 'Count vowels', runner: 'python',
    prompt: 'Write `count_vowels(text)` that returns how many vowels (a, e, i, o, u — any case) appear in `text`.',
    starter: 'def count_vowels(text):\n    pass\n', hints: ['Lower-case the text first, then loop or use a generator with `sum`.'],
    tests: [{ call: 'count_vowels("Title Insurance")', expect: '6' }, { call: 'count_vowels("XYZ")', expect: '0' }, { call: 'count_vowels("AEIOU aeiou")', expect: '10' }],
    solution: 'def count_vowels(text):\n    return sum(1 for ch in text.lower() if ch in "aeiou")' },
  { id: 'py-fizz', cat: 'Python', diff: 'easy', title: 'FizzBuzz list', runner: 'python',
    prompt: 'Write `fizzbuzz(n)` returning a list of strings for 1..n: multiples of 3 → "Fizz", of 5 → "Buzz", of both → "FizzBuzz", otherwise the number as a string.',
    starter: 'def fizzbuzz(n):\n    pass\n', hints: ['Check the 15 case first.'],
    tests: [{ call: 'fizzbuzz(5)', expect: '["1","2","Fizz","4","Buzz"]' }, { call: 'fizzbuzz(15)[-1]', expect: '"FizzBuzz"' }, { call: 'len(fizzbuzz(0))', expect: '0' }],
    solution: 'def fizzbuzz(n):\n    out = []\n    for i in range(1, n + 1):\n        if i % 15 == 0: out.append("FizzBuzz")\n        elif i % 3 == 0: out.append("Fizz")\n        elif i % 5 == 0: out.append("Buzz")\n        else: out.append(str(i))\n    return out' },
  { id: 'py-wordfreq', cat: 'Python', diff: 'medium', title: 'Word frequency', runner: 'python',
    prompt: 'Write `word_freq(text)` returning a dict of lower-cased word → count. Strip punctuation `.,;:!?` from words.',
    starter: 'def word_freq(text):\n    pass\n', hints: ['`str.strip(".,;:!?")` removes leading/trailing punctuation.', '`collections.Counter` is a dict subclass, so `== {...}` works.'],
    tests: [{ call: 'word_freq("The deed, the deed.")', expect: '{"the": 2, "deed": 2}' }, { call: 'word_freq("")', expect: '{}' }, { call: 'word_freq("A a A!")["a"]', expect: '3' }],
    solution: 'from collections import Counter\n\ndef word_freq(text):\n    words = [w.strip(".,;:!?").lower() for w in text.split()]\n    return dict(Counter(w for w in words if w))' },
  { id: 'py-csvparse', cat: 'Python', diff: 'medium', title: 'Parse a CSV string into totals', runner: 'python',
    prompt: 'Given a CSV string with header `state,files`, write `totals(csv_text)` that returns a dict of state → total files (states may repeat).\n\n```text\nstate,files\nWyoming,120\nTennessee,300\nWyoming,30\n```\n→ `{"Wyoming": 150, "Tennessee": 300}`',
    starter: 'import csv, io\n\ndef totals(csv_text):\n    pass\n', hints: ['`csv.DictReader(io.StringIO(csv_text))` yields dicts.'],
    tests: [{ call: 'totals("state,files\\nWyoming,120\\nTennessee,300\\nWyoming,30")', expect: '{"Wyoming": 150, "Tennessee": 300}' }, { call: 'totals("state,files")', expect: '{}' }],
    solution: 'import csv, io\n\ndef totals(csv_text):\n    out = {}\n    for row in csv.DictReader(io.StringIO(csv_text)):\n        out[row["state"]] = out.get(row["state"], 0) + int(row["files"])\n    return out' },
  { id: 'py-dates', cat: 'Python', diff: 'medium', title: 'Business days between dates', runner: 'python',
    prompt: 'Write `business_days(start, end)` taking ISO date strings and returning the number of weekdays (Mon–Fri) from `start` to `end` inclusive. Turnaround-time (TAT) reports need exactly this.',
    starter: 'from datetime import date, timedelta\n\ndef business_days(start, end):\n    pass\n', hints: ['`date.fromisoformat("2026-01-05")`', '`d.weekday()` is 0 for Monday and 6 for Sunday.'],
    tests: [{ call: 'business_days("2026-01-05", "2026-01-09")', expect: '5' }, { call: 'business_days("2026-01-03", "2026-01-04")', expect: '0' }, { call: 'business_days("2026-01-01", "2026-01-31")', expect: '22' }],
    solution: 'from datetime import date, timedelta\n\ndef business_days(start, end):\n    d, e = date.fromisoformat(start), date.fromisoformat(end)\n    n = 0\n    while d <= e:\n        if d.weekday() < 5:\n            n += 1\n        d += timedelta(days=1)\n    return n' },
  { id: 'py-flatten', cat: 'Python', diff: 'medium', title: 'Flatten nested lists', runner: 'python',
    prompt: 'Write `flatten(items)` that flattens arbitrarily nested lists into one flat list, preserving order.',
    starter: 'def flatten(items):\n    pass\n', hints: ['Recursion: if an element is a list, extend with `flatten(element)`.'],
    tests: [{ call: 'flatten([1, [2, [3, 4]], 5])', expect: '[1, 2, 3, 4, 5]' }, { call: 'flatten([])', expect: '[]' }, { call: 'flatten([[[]], [1]])', expect: '[1]' }],
    solution: 'def flatten(items):\n    out = []\n    for x in items:\n        if isinstance(x, list):\n            out.extend(flatten(x))\n        else:\n            out.append(x)\n    return out' },
  { id: 'py-class-invoice', cat: 'Python', diff: 'medium', title: 'Invoice class', runner: 'python',
    prompt: 'Create a class `Invoice` with `add(description, qty, unit_price)` and a `total` property. Also implement `__len__` returning the number of line items.',
    starter: 'class Invoice:\n    def __init__(self):\n        self.items = []\n', hints: ['Use `@property` for `total`.'],
    tests: [{ name: 'total of two lines', call: '(lambda i: (i.add("Formatting", 2, 50), i.add("Cover", 1, 120), i.total)[-1])(Invoice())', expect: '220' }, { name: 'len() works', call: '(lambda i: (i.add("A", 1, 1), len(i))[-1])(Invoice())', expect: '1' }, { name: 'empty invoice totals 0', call: 'Invoice().total', expect: '0' }],
    solution: 'class Invoice:\n    def __init__(self):\n        self.items = []\n\n    def add(self, description, qty, unit_price):\n        self.items.append((description, qty, unit_price))\n\n    @property\n    def total(self):\n        return sum(q * p for _, q, p in self.items)\n\n    def __len__(self):\n        return len(self.items)' },
  { id: 'py-decorator', cat: 'Python', diff: 'hard', title: 'Write a memoize decorator', runner: 'python',
    prompt: 'Write a decorator `memoize` that caches results of a function by its positional arguments. Then define `fib(n)` decorated with it so `fib(60)` is instant.',
    starter: 'def memoize(fn):\n    pass\n\n@memoize\ndef fib(n):\n    return n if n < 2 else fib(n - 1) + fib(n - 2)\n', hints: ['Keep a dict in the closure; key on `args`.', 'Use `functools.wraps` to keep the name.'],
    tests: [{ call: 'fib(60)', expect: '1548008755920' }, { call: 'fib(0)', expect: '0' }, { call: 'fib.__name__', expect: '"fib"' }],
    solution: 'import functools\n\ndef memoize(fn):\n    cache = {}\n    @functools.wraps(fn)\n    def wrapper(*args):\n        if args not in cache:\n            cache[args] = fn(*args)\n        return cache[args]\n    return wrapper\n\n@memoize\ndef fib(n):\n    return n if n < 2 else fib(n - 1) + fib(n - 2)' },
  { id: 'py-generator', cat: 'Python', diff: 'hard', title: 'Chunk a generator', runner: 'python',
    prompt: 'Write `chunks(iterable, size)` — a generator that yields lists of at most `size` items from any iterable (including generators), without loading the whole iterable into memory.',
    starter: 'def chunks(iterable, size):\n    pass\n', hints: ['`iter()` + `itertools.islice` is the idiomatic tool.'],
    tests: [{ call: 'list(chunks(range(7), 3))', expect: '[[0, 1, 2], [3, 4, 5], [6]]' }, { call: 'list(chunks([], 2))', expect: '[]' }, { call: 'list(chunks((x for x in "abcd"), 2))', expect: '[["a", "b"], ["c", "d"]]' }],
    solution: 'from itertools import islice\n\ndef chunks(iterable, size):\n    it = iter(iterable)\n    while True:\n        block = list(islice(it, size))\n        if not block:\n            return\n        yield block' },
  { id: 'py-regex-fields', cat: 'Python', diff: 'hard', title: 'Extract field codes with regex', runner: 'python',
    prompt: 'Word field codes look like `{ PAGE }`, `{ DOCPROPERTY Title \\* MERGEFORMAT }`. Write `field_names(text)` returning the list of field keywords (first word inside each brace) in order.',
    starter: 'import re\n\ndef field_names(text):\n    pass\n', hints: ['Pattern: `\\{\\s*([A-Z]+)`'],
    tests: [{ call: 'field_names("Page { PAGE } of { NUMPAGES } — { DOCPROPERTY Title \\\\* MERGEFORMAT }")', expect: '["PAGE", "NUMPAGES", "DOCPROPERTY"]' }, { call: 'field_names("no fields")', expect: '[]' }],
    solution: 'import re\n\ndef field_names(text):\n    return re.findall(r"\\{\\s*([A-Z]+)", text)' },
  { id: 'py-docx-build', cat: 'Document Automation', diff: 'medium', title: 'Build a DOCX with python-docx', runner: 'python', packages: 'python-docx',
    prompt: 'Write `build_doc(title, bullets)` that returns a `docx.Document` with a Heading level 1 of `title` followed by one paragraph per bullet using the built-in style `List Bullet`.',
    starter: 'from docx import Document\n\ndef build_doc(title, bullets):\n    doc = Document()\n    # add heading + bullet paragraphs\n    return doc\n', hints: ['`doc.add_heading(title, level=1)`', '`doc.add_paragraph(text, style="List Bullet")`'],
    tests: [{ name: 'heading is first paragraph', call: 'build_doc("Policy Manual", ["a", "b"]).paragraphs[0].text', expect: '"Policy Manual"' }, { name: 'heading style', call: 'build_doc("T", []).paragraphs[0].style.name', expect: '"Heading 1"' }, { name: 'bullets use List Bullet', call: '[p.style.name for p in build_doc("T", ["a", "b"]).paragraphs[1:]]', expect: '["List Bullet", "List Bullet"]' }],
    solution: 'from docx import Document\n\ndef build_doc(title, bullets):\n    doc = Document()\n    doc.add_heading(title, level=1)\n    for b in bullets:\n        doc.add_paragraph(b, style="List Bullet")\n    return doc' },
  { id: 'py-docx-table', cat: 'Document Automation', diff: 'hard', title: 'Data table in DOCX', runner: 'python', packages: 'python-docx',
    prompt: 'Write `rate_table(rows)` where `rows` is a list of dicts with keys `state`, `policy`, `rate`. Return a Document containing one table with a header row (State, Policy, Rate) and one row per dict, style `Table Grid`, rate formatted as `$1,234.50`.',
    starter: 'from docx import Document\n\ndef rate_table(rows):\n    doc = Document()\n    return doc\n', hints: ['`t = doc.add_table(rows=1, cols=3); t.style = "Table Grid"`', '`f"${rate:,.2f}"`'],
    tests: [{ name: 'one table', call: 'len(rate_table([{"state":"WY","policy":"Owner","rate":1234.5}]).tables)', expect: '1' }, { name: 'header + rows', call: 'len(rate_table([{"state":"WY","policy":"Owner","rate":1},{"state":"TN","policy":"Loan","rate":2}]).tables[0].rows)', expect: '3' }, { name: 'formatted rate', call: 'rate_table([{"state":"WY","policy":"Owner","rate":1234.5}]).tables[0].cell(1,2).text', expect: '"$1,234.50"' }],
    solution: 'from docx import Document\n\ndef rate_table(rows):\n    doc = Document()\n    t = doc.add_table(rows=1, cols=3)\n    t.style = "Table Grid"\n    for c, h in zip(t.rows[0].cells, ["State", "Policy", "Rate"]):\n        c.text = h\n    for r in rows:\n        cells = t.add_row().cells\n        cells[0].text = r["state"]\n        cells[1].text = r["policy"]\n        cells[2].text = f"${r[\'rate\']:,.2f}"\n    return doc' },
  { id: 'py-openpyxl', cat: 'Document Automation', diff: 'medium', title: 'Workbook with a SUM formula', runner: 'python', packages: 'openpyxl',
    prompt: 'Write `report(values)` returning an openpyxl Workbook whose active sheet has "Files" in A1, the values in A2..An+1, and a `=SUM(A2:An+1)` formula in the cell right after the last value. Column A should also be bold in row 1.',
    starter: 'from openpyxl import Workbook\nfrom openpyxl.styles import Font\n\ndef report(values):\n    wb = Workbook()\n    ws = wb.active\n    return wb\n', hints: ['`ws.cell(row=i, column=1, value=v)`', '`ws["A1"].font = Font(bold=True)`'],
    tests: [{ call: 'report([1,2,3]).active["A1"].value', expect: '"Files"' }, { call: 'report([1,2,3]).active["A5"].value', expect: '"=SUM(A2:A4)"' }, { call: 'report([5]).active["A1"].font.bold', expect: 'True' }],
    solution: 'from openpyxl import Workbook\nfrom openpyxl.styles import Font\n\ndef report(values):\n    wb = Workbook()\n    ws = wb.active\n    ws["A1"] = "Files"\n    ws["A1"].font = Font(bold=True)\n    for i, v in enumerate(values, start=2):\n        ws.cell(row=i, column=1, value=v)\n    last = len(values) + 1\n    ws.cell(row=last + 1, column=1, value=f"=SUM(A2:A{last})")\n    return wb' },
  { id: 'py-ooxml-zip', cat: 'Document Automation', diff: 'hard', title: 'Read text straight from DOCX XML', runner: 'python',
    prompt: 'A `.docx` is a ZIP. Write `docx_text(data: bytes)` that opens the ZIP from bytes, reads `word/document.xml` and returns all `<w:t>` text joined with spaces — no python-docx allowed. A minimal DOCX builder is provided for the tests.',
    starter: 'import zipfile, io, re\nimport xml.etree.ElementTree as ET\n\nW = "http://schemas.openxmlformats.org/wordprocessingml/2006/main"\n\ndef docx_text(data):\n    pass\n\n# --- helper used by the tests (do not edit) ---\ndef make_docx(paragraphs):\n    buf = io.BytesIO()\n    body = "".join(f"<w:p><w:r><w:t>{p}</w:t></w:r></w:p>" for p in paragraphs)\n    with zipfile.ZipFile(buf, "w") as z:\n        z.writestr("[Content_Types].xml", \'<?xml version="1.0"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="xml" ContentType="application/xml"/><Override PartName="/word/document.xml" ContentType="application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml"/></Types>\')\n        z.writestr("word/document.xml", f\'<?xml version="1.0"?><w:document xmlns:w="{W}"><w:body>{body}</w:body></w:document>\')\n    return buf.getvalue()\n',
    hints: ['`zipfile.ZipFile(io.BytesIO(data)).read("word/document.xml")`', '`root.iter("{%s}t" % W)` walks every w:t element.'],
    tests: [{ call: 'docx_text(make_docx(["Hello", "World"]))', expect: '"Hello World"' }, { call: 'docx_text(make_docx([]))', expect: '""' }],
    solution: 'def docx_text(data):\n    with zipfile.ZipFile(io.BytesIO(data)) as z:\n        xml = z.read("word/document.xml")\n    root = ET.fromstring(xml)\n    return " ".join(t.text or "" for t in root.iter("{%s}t" % W))' },

  /* ===================== PANDAS / DATA ===================== */
  { id: 'pd-clean', cat: 'pandas & Data Cleaning', diff: 'easy', title: 'Standardise state names', runner: 'python',
    prompt: 'Write `clean_states(values)` that takes a list of messy state strings (extra spaces, mixed case, e.g. `" wyoming "`) and returns a list of title-cased, stripped strings.',
    starter: 'def clean_states(values):\n    pass\n', hints: ['`s.strip().title()`'],
    tests: [{ call: 'clean_states([" wyoming ", "TENNESSEE", "texas"])', expect: '["Wyoming", "Tennessee", "Texas"]' }, { call: 'clean_states([])', expect: '[]' }],
    solution: 'def clean_states(values):\n    return [v.strip().title() for v in values]' },
  { id: 'pd-groupby', cat: 'pandas & Data Cleaning', diff: 'medium', title: 'Files per state with pandas', runner: 'python',
    prompt: 'Write `files_by_state(records)` where `records` is a list of dicts with `state` and `files`. Use pandas to return a dict of state → total files, sorted by state name.',
    starter: 'import pandas as pd\n\ndef files_by_state(records):\n    pass\n', hints: ['`pd.DataFrame(records).groupby("state")["files"].sum()` then `.sort_index().to_dict()`.'],
    tests: [{ call: 'files_by_state([{"state":"WY","files":2},{"state":"TN","files":5},{"state":"WY","files":3}])', expect: '{"TN": 5, "WY": 5}' }],
    solution: 'import pandas as pd\n\ndef files_by_state(records):\n    df = pd.DataFrame(records)\n    return {k: int(v) for k, v in df.groupby("state")["files"].sum().sort_index().items()}' },
  { id: 'pd-dedupe', cat: 'pandas & Data Cleaning', diff: 'medium', title: 'Deduplicate keeping the latest row', runner: 'python',
    prompt: 'Write `latest(records)` where each record has `file_no`, `status`, `updated` (ISO date string). Return a list of dicts with only the most recent row per `file_no`, sorted by `file_no`.',
    starter: 'import pandas as pd\n\ndef latest(records):\n    pass\n', hints: ['Sort by `updated`, then `drop_duplicates("file_no", keep="last")`.'],
    tests: [{ call: 'latest([{"file_no":"A1","status":"open","updated":"2026-01-01"},{"file_no":"A1","status":"closed","updated":"2026-02-01"},{"file_no":"A0","status":"open","updated":"2026-01-05"}])', expect: '[{"file_no":"A0","status":"open","updated":"2026-01-05"},{"file_no":"A1","status":"closed","updated":"2026-02-01"}]' }],
    solution: 'import pandas as pd\n\ndef latest(records):\n    df = pd.DataFrame(records).sort_values("updated")\n    df = df.drop_duplicates("file_no", keep="last").sort_values("file_no")\n    return df.to_dict("records")' },
  { id: 'pd-validate', cat: 'pandas & Data Cleaning', diff: 'hard', title: 'Validation report', runner: 'python',
    prompt: 'Write `validate(rows)` for a rate matrix. Each row has `zone` (must be 1–5), `rate` (must be > 0) and `underwriter` (non-empty). Return a list of strings `"row <i>: <field> invalid"` for every failing field (row index starting at 0, fields checked in the order zone, rate, underwriter).',
    starter: 'def validate(rows):\n    pass\n', hints: ['Loop with `enumerate` and append messages in order.'],
    tests: [{ call: 'validate([{"zone":1,"rate":10,"underwriter":"Stewart"},{"zone":9,"rate":-1,"underwriter":""}])', expect: '["row 1: zone invalid", "row 1: rate invalid", "row 1: underwriter invalid"]' }, { call: 'validate([])', expect: '[]' }],
    solution: 'def validate(rows):\n    errs = []\n    for i, r in enumerate(rows):\n        if not (1 <= r.get("zone", 0) <= 5):\n            errs.append(f"row {i}: zone invalid")\n        if not (r.get("rate", 0) > 0):\n            errs.append(f"row {i}: rate invalid")\n        if not r.get("underwriter"):\n            errs.append(f"row {i}: underwriter invalid")\n    return errs' },
  { id: 'pd-pivot', cat: 'pandas & Data Cleaning', diff: 'hard', title: 'Pivot a weekly report', runner: 'python',
    prompt: 'Write `pivot(records)` where records have `week`, `state`, `files`. Return a nested dict `{week: {state: files}}` with missing combinations filled with 0, using `pandas.pivot_table`.',
    starter: 'import pandas as pd\n\ndef pivot(records):\n    pass\n', hints: ['`pd.pivot_table(df, index="week", columns="state", values="files", aggfunc="sum", fill_value=0)`', '`.to_dict("index")` gives {row: {col: value}}.'],
    tests: [{ call: 'pivot([{"week":"W1","state":"WY","files":2},{"week":"W1","state":"TN","files":1},{"week":"W2","state":"WY","files":4}])', expect: '{"W1": {"TN": 1, "WY": 2}, "W2": {"TN": 0, "WY": 4}}' }],
    solution: 'import pandas as pd\n\ndef pivot(records):\n    df = pd.DataFrame(records)\n    pt = pd.pivot_table(df, index="week", columns="state", values="files", aggfunc="sum", fill_value=0)\n    return {w: {s: int(v) for s, v in row.items()} for w, row in pt.to_dict("index").items()}' },

  /* ===================== DSA ===================== */
  { id: 'dsa-twosum', cat: 'DSA', diff: 'easy', title: 'Two Sum', runner: 'python',
    prompt: 'Write `two_sum(nums, target)` returning the indices `[i, j]` (i < j) of the two numbers that add to `target`. Assume exactly one solution. Aim for O(n).',
    starter: 'def two_sum(nums, target):\n    pass\n', hints: ['Store seen values in a dict value → index.'],
    tests: [{ call: 'two_sum([2, 7, 11, 15], 9)', expect: '[0, 1]' }, { call: 'two_sum([3, 2, 4], 6)', expect: '[1, 2]' }],
    solution: 'def two_sum(nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n        if target - n in seen:\n            return [seen[target - n], i]\n        seen[n] = i' },
  { id: 'dsa-parens', cat: 'DSA', diff: 'easy', title: 'Valid brackets', runner: 'python',
    prompt: 'Write `is_valid(s)` that returns True if the brackets `()[]{}` in `s` are balanced and correctly nested.',
    starter: 'def is_valid(s):\n    pass\n', hints: ['Use a stack (a Python list).'],
    tests: [{ call: 'is_valid("()[]{}")', expect: 'True' }, { call: 'is_valid("([)]")', expect: 'False' }, { call: 'is_valid("{[]}")', expect: 'True' }, { call: 'is_valid("(")', expect: 'False' }],
    solution: 'def is_valid(s):\n    pairs = {")": "(", "]": "[", "}": "{"}\n    stack = []\n    for ch in s:\n        if ch in "([{":\n            stack.append(ch)\n        elif ch in pairs:\n            if not stack or stack.pop() != pairs[ch]:\n                return False\n    return not stack' },
  { id: 'dsa-binsearch', cat: 'DSA', diff: 'medium', title: 'Binary search', runner: 'python',
    prompt: 'Write `binary_search(sorted_list, target)` returning the index of `target` or -1. Must be O(log n) — no `list.index`.',
    starter: 'def binary_search(sorted_list, target):\n    pass\n', hints: ['Maintain `lo` and `hi`; compare with the middle element.'],
    tests: [{ call: 'binary_search([1, 3, 5, 7, 9, 11], 7)', expect: '3' }, { call: 'binary_search([1, 3, 5], 4)', expect: '-1' }, { call: 'binary_search([], 1)', expect: '-1' }],
    solution: 'def binary_search(sorted_list, target):\n    lo, hi = 0, len(sorted_list) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if sorted_list[mid] == target:\n            return mid\n        if sorted_list[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1' },
  { id: 'dsa-anagram', cat: 'DSA', diff: 'medium', title: 'Group anagrams', runner: 'python',
    prompt: 'Write `group_anagrams(words)` returning a list of groups (each group a sorted list of words), groups sorted by their first word.',
    starter: 'def group_anagrams(words):\n    pass\n', hints: ['Key each word by `"".join(sorted(word))`.'],
    tests: [{ call: 'group_anagrams(["eat","tea","tan","ate","nat","bat"])', expect: '[["ate","eat","tea"],["bat"],["nat","tan"]]' }],
    solution: 'from collections import defaultdict\n\ndef group_anagrams(words):\n    groups = defaultdict(list)\n    for w in words:\n        groups["".join(sorted(w))].append(w)\n    return sorted(sorted(g) for g in groups.values())' },
  { id: 'dsa-merge-intervals', cat: 'DSA', diff: 'hard', title: 'Merge intervals', runner: 'python',
    prompt: 'Write `merge(intervals)` that merges overlapping `[start, end]` intervals and returns them sorted.',
    starter: 'def merge(intervals):\n    pass\n', hints: ['Sort by start, then extend the last merged interval when the next one overlaps.'],
    tests: [{ call: 'merge([[1,3],[2,6],[8,10],[15,18]])', expect: '[[1,6],[8,10],[15,18]]' }, { call: 'merge([[1,4],[4,5]])', expect: '[[1,5]]' }, { call: 'merge([])', expect: '[]' }],
    solution: 'def merge(intervals):\n    out = []\n    for s, e in sorted(intervals):\n        if out and s <= out[-1][1]:\n            out[-1][1] = max(out[-1][1], e)\n        else:\n            out.append([s, e])\n    return out' },
  { id: 'dsa-lru', cat: 'DSA', diff: 'hard', title: 'LRU cache', runner: 'python',
    prompt: 'Implement class `LRUCache(capacity)` with `get(key)` (returns -1 if missing) and `put(key, value)`. Both must be O(1). Evict the least recently used key when full.',
    starter: 'from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity):\n        pass\n\n    def get(self, key):\n        pass\n\n    def put(self, key, value):\n        pass\n', hints: ['`OrderedDict.move_to_end(key)` and `popitem(last=False)`.'],
    tests: [{ name: 'evicts LRU', call: '(lambda c: (c.put(1,1), c.put(2,2), c.get(1), c.put(3,3), c.get(2))[-1])(LRUCache(2))', expect: '-1' }, { name: 'get after put', call: '(lambda c: (c.put(1,10), c.get(1))[-1])(LRUCache(1))', expect: '10' }],
    solution: 'from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity):\n        self.cap = capacity\n        self.d = OrderedDict()\n\n    def get(self, key):\n        if key not in self.d:\n            return -1\n        self.d.move_to_end(key)\n        return self.d[key]\n\n    def put(self, key, value):\n        if key in self.d:\n            self.d.move_to_end(key)\n        self.d[key] = value\n        if len(self.d) > self.cap:\n            self.d.popitem(last=False)' },

  /* ===================== JAVASCRIPT ===================== */
  { id: 'js-capitalize', cat: 'JavaScript', diff: 'easy', title: 'Capitalise each word', runner: 'js',
    prompt: 'Write `titleCase(str)` that upper-cases the first letter of every word and lower-cases the rest.',
    starter: 'function titleCase(str) {\n  // your code\n}\n', hints: ['`str.split(" ").map(...)`'],
    tests: [{ call: 'titleCase("stewart title production")', expect: '"Stewart Title Production"' }, { call: 'titleCase("WYOMING")', expect: '"Wyoming"' }],
    solution: 'function titleCase(str) {\n  return str.split(" ").map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(" ");\n}' },
  { id: 'js-sumby', cat: 'JavaScript', diff: 'easy', title: 'Sum by key with reduce', runner: 'js',
    prompt: 'Write `sumBy(items, key)` that sums the numeric property `key` across an array of objects.',
    starter: 'function sumBy(items, key) {\n}\n', hints: ['`items.reduce((acc, it) => acc + it[key], 0)`'],
    tests: [{ call: 'sumBy([{files: 2}, {files: 5}], "files")', expect: '7' }, { call: 'sumBy([], "x")', expect: '0' }],
    solution: 'function sumBy(items, key) {\n  return items.reduce((acc, it) => acc + (Number(it[key]) || 0), 0);\n}' },
  { id: 'js-groupby', cat: 'JavaScript', diff: 'medium', title: 'Group records by state', runner: 'js',
    prompt: 'Write `groupBy(items, key)` returning an object whose keys are the distinct values of `key` and whose values are arrays of the matching items.',
    starter: 'function groupBy(items, key) {\n}\n', hints: ['`(acc[it[key]] ||= []).push(it)`'],
    tests: [{ call: 'groupBy([{s:"WY",n:1},{s:"TN",n:2},{s:"WY",n:3}], "s")', expect: '{WY:[{s:"WY",n:1},{s:"WY",n:3}],TN:[{s:"TN",n:2}]}' }],
    solution: 'function groupBy(items, key) {\n  return items.reduce((acc, it) => { (acc[it[key]] ||= []).push(it); return acc; }, {});\n}' },
  { id: 'js-debounce', cat: 'JavaScript', diff: 'medium', title: 'Debounce', runner: 'js',
    prompt: 'Write `debounce(fn, ms)` returning a function that calls `fn` only after `ms` milliseconds have passed without another call. The test calls it 3 times quickly and expects exactly one invocation.',
    starter: 'function debounce(fn, ms) {\n}\n', hints: ['Keep a timer id in the closure; `clearTimeout` then `setTimeout`.'],
    tests: [{ name: 'called once after burst', call: 'new Promise(res => { let n = 0; const d = debounce(() => n++, 30); d(); d(); d(); setTimeout(() => res(n), 120); })', expect: '1' }],
    solution: 'function debounce(fn, ms) {\n  let t;\n  return function (...args) {\n    clearTimeout(t);\n    t = setTimeout(() => fn.apply(this, args), ms);\n  };\n}' },
  { id: 'js-rate-calc', cat: 'JavaScript', diff: 'medium', title: 'Tiered rate calculator', runner: 'js',
    prompt: 'Title-insurance premiums are often tiered: the first $100,000 at $5.75 per $1,000, the next $400,000 at $4.50 per $1,000, and everything above at $3.00 per $1,000. Write `premium(amount)` that returns the premium rounded to 2 decimals.',
    starter: 'function premium(amount) {\n}\n', hints: ['Process tiers in order, subtracting what each tier consumed.'],
    tests: [{ call: 'premium(100000)', expect: '575' }, { call: 'premium(250000)', expect: '1250' }, { call: 'premium(1000000)', expect: '3875' }],
    solution: 'function premium(amount) {\n  const tiers = [[100000, 5.75], [400000, 4.5], [Infinity, 3.0]];\n  let left = amount, total = 0;\n  for (const [size, rate] of tiers) {\n    const slice = Math.min(left, size);\n    total += slice / 1000 * rate;\n    left -= slice;\n    if (left <= 0) break;\n  }\n  return Math.round(total * 100) / 100;\n}' },
  { id: 'js-promise-all', cat: 'JavaScript', diff: 'hard', title: 'Retry with backoff', runner: 'js',
    prompt: 'Write `retry(fn, attempts)` that calls the async function `fn` until it resolves, up to `attempts` times, and rejects with the last error if all fail. The test provides a function that fails twice then succeeds.',
    starter: 'async function retry(fn, attempts) {\n}\n', hints: ['A for-loop with try/catch inside an async function.'],
    tests: [{ name: 'succeeds on third try', call: '(() => { let n = 0; return retry(async () => { if (++n < 3) throw new Error("fail"); return "ok:" + n; }, 5); })()', expect: '"ok:3"' }, { name: 'rejects after attempts', call: 'retry(async () => { throw new Error("nope"); }, 2).then(() => "resolved", e => e.message)', expect: '"nope"' }],
    solution: 'async function retry(fn, attempts) {\n  let last;\n  for (let i = 0; i < attempts; i++) {\n    try { return await fn(); } catch (e) { last = e; }\n  }\n  throw last;\n}' },
  { id: 'js-truncation', cat: 'JavaScript', diff: 'hard', title: 'Detect truncated JSON output', runner: 'js',
    prompt: 'LLM outputs get cut off when they hit max_tokens. Write `isTruncated(text)` that returns true if the text is not valid JSON *and* its brackets/braces are unbalanced (more openers than closers, ignoring characters inside strings).',
    starter: 'function isTruncated(text) {\n}\n', hints: ['Try `JSON.parse` first.', 'Walk the characters tracking `inString` and a depth counter.'],
    tests: [{ call: 'isTruncated(\'{"rows": [{"a": 1}, {"a": 2\')', expect: 'true' }, { call: 'isTruncated(\'{"rows": [{"a": "}"}]}\')', expect: 'false' }, { call: 'isTruncated("[]")', expect: 'false' }],
    solution: 'function isTruncated(text) {\n  try { JSON.parse(text); return false; } catch (e) {}\n  let depth = 0, inStr = false, esc = false;\n  for (const ch of text) {\n    if (inStr) { if (esc) esc = false; else if (ch === "\\\\") esc = true; else if (ch === \'"\') inStr = false; continue; }\n    if (ch === \'"\') inStr = true;\n    else if (ch === "{" || ch === "[") depth++;\n    else if (ch === "}" || ch === "]") depth--;\n  }\n  return depth > 0 || inStr;\n}' },

  /* ===================== SQL ===================== */
  { id: 'sql-select', cat: 'SQL', diff: 'easy', title: 'Files over 100', runner: 'sql',
    prompt: 'Table `production(state TEXT, files INTEGER)`. Return `state` and `files` for rows with more than 100 files, ordered by files descending.',
    setup: 'CREATE TABLE production(state TEXT, files INTEGER); INSERT INTO production VALUES ("Wyoming",120),("Tennessee",340),("Texas",90),("Florida",210);',
    starter: 'SELECT ...', expect: [['Tennessee', 340], ['Florida', 210], ['Wyoming', 120]],
    solution: 'SELECT state, files FROM production WHERE files > 100 ORDER BY files DESC;' },
  { id: 'sql-group', cat: 'SQL', diff: 'easy', title: 'Total files per state', runner: 'sql',
    prompt: 'Table `orders(state TEXT, files INTEGER)` has several rows per state. Return `state` and the total as `total`, ordered by state.',
    setup: 'CREATE TABLE orders(state TEXT, files INTEGER); INSERT INTO orders VALUES ("WY",10),("TN",5),("WY",15),("TN",20),("TX",1);',
    starter: 'SELECT ...', expect: [['TN', 25], ['TX', 1], ['WY', 25]],
    solution: 'SELECT state, SUM(files) AS total FROM orders GROUP BY state ORDER BY state;' },
  { id: 'sql-join', cat: 'SQL', diff: 'medium', title: 'Agents and their teams', runner: 'sql',
    prompt: 'Tables `agents(id, name, team_id)` and `teams(id, name)`. Return every agent name with their team name (`agent`, `team`), including agents with no team (show NULL), ordered by agent name.',
    setup: 'CREATE TABLE teams(id INTEGER, name TEXT); CREATE TABLE agents(id INTEGER, name TEXT, team_id INTEGER); INSERT INTO teams VALUES (1,"Title Search"),(2,"QC"); INSERT INTO agents VALUES (1,"Ali",1),(2,"Sara",2),(3,"Bilal",NULL);',
    starter: 'SELECT ...', expect: [['Ali', 'Title Search'], ['Bilal', null], ['Sara', 'QC']],
    solution: 'SELECT a.name AS agent, t.name AS team\nFROM agents a\nLEFT JOIN teams t ON t.id = a.team_id\nORDER BY a.name;' },
  { id: 'sql-having', cat: 'SQL', diff: 'medium', title: 'Agents below the accuracy target', runner: 'sql',
    prompt: 'Table `qc(agent TEXT, checked INTEGER, errors INTEGER)` has one row per day. Return `agent` and accuracy as `acc` = 1 - SUM(errors)/SUM(checked) (as a decimal rounded to 2 places) for agents whose accuracy is below 0.98, ordered by acc ascending.',
    setup: 'CREATE TABLE qc(agent TEXT, checked INTEGER, errors INTEGER); INSERT INTO qc VALUES ("Ali",100,1),("Ali",100,0),("Sara",100,5),("Sara",100,3),("Bilal",50,2),("Bilal",50,0);',
    starter: 'SELECT ...', expect: [['Sara', 0.96], ['Bilal', 0.98]],
    solution: 'SELECT agent, ROUND(1.0 - SUM(errors) * 1.0 / SUM(checked), 2) AS acc\nFROM qc\nGROUP BY agent\nHAVING acc < 0.985\nORDER BY acc;',
    explain: 'Note the `* 1.0` — without it SQLite performs integer division and every accuracy becomes 1. The HAVING threshold uses 0.985 so that 0.98 (Bilal) is included after rounding; interviewers love asking why the rounded and raw values can disagree.' },
  { id: 'sql-window', cat: 'SQL', diff: 'hard', title: 'Rank states by weekly files', runner: 'sql',
    prompt: 'Table `weekly(week TEXT, state TEXT, files INTEGER)`. For each week, rank states by files (1 = most). Return `week`, `state`, `rnk` ordered by week then rnk.',
    setup: 'CREATE TABLE weekly(week TEXT, state TEXT, files INTEGER); INSERT INTO weekly VALUES ("W1","WY",10),("W1","TN",30),("W1","TX",20),("W2","WY",50),("W2","TN",40);',
    starter: 'SELECT ...', expect: [['W1', 'TN', 1], ['W1', 'TX', 2], ['W1', 'WY', 3], ['W2', 'WY', 1], ['W2', 'TN', 2]],
    solution: 'SELECT week, state, RANK() OVER (PARTITION BY week ORDER BY files DESC) AS rnk\nFROM weekly\nORDER BY week, rnk;' },
  { id: 'sql-running', cat: 'SQL', diff: 'hard', title: 'Running total of files', runner: 'sql',
    prompt: 'Table `daily(day TEXT, files INTEGER)`. Return `day`, `files` and a running total `cum` ordered by day.',
    setup: 'CREATE TABLE daily(day TEXT, files INTEGER); INSERT INTO daily VALUES ("2026-01-01",5),("2026-01-02",7),("2026-01-03",3);',
    starter: 'SELECT ...', expect: [['2026-01-01', 5, 5], ['2026-01-02', 7, 12], ['2026-01-03', 3, 15]],
    solution: 'SELECT day, files, SUM(files) OVER (ORDER BY day) AS cum FROM daily ORDER BY day;' },
  { id: 'sql-dedupe', cat: 'SQL', diff: 'hard', title: 'Latest status per file', runner: 'sql',
    prompt: 'Table `events(file_no TEXT, status TEXT, at TEXT)`. Return `file_no` and its latest `status` (by `at`), ordered by file_no.',
    setup: 'CREATE TABLE events(file_no TEXT, status TEXT, at TEXT); INSERT INTO events VALUES ("A1","open","2026-01-01"),("A1","closed","2026-02-01"),("A2","open","2026-01-05"),("A2","hold","2026-01-03");',
    starter: 'SELECT ...', expect: [['A1', 'closed'], ['A2', 'open']],
    solution: 'WITH ranked AS (\n  SELECT file_no, status, ROW_NUMBER() OVER (PARTITION BY file_no ORDER BY at DESC) AS rn\n  FROM events\n)\nSELECT file_no, status FROM ranked WHERE rn = 1 ORDER BY file_no;' },
  { id: 'sql-cte', cat: 'SQL', diff: 'medium', title: 'Above-average states', runner: 'sql',
    prompt: 'Table `production(state TEXT, files INTEGER)`. Using a CTE or subquery, return the states whose files are above the overall average, ordered by state.',
    setup: 'CREATE TABLE production(state TEXT, files INTEGER); INSERT INTO production VALUES ("Wyoming",120),("Tennessee",340),("Texas",90),("Florida",210);',
    starter: 'SELECT ...', expect: [['Florida'], ['Tennessee']],
    solution: 'WITH avg_files AS (SELECT AVG(files) AS a FROM production)\nSELECT state FROM production, avg_files WHERE files > a ORDER BY state;' },

  /* ===================== EXCEL / POWER BI (self-check) ===================== */
  { id: 'xl-sumifs', cat: 'Excel', diff: 'easy', title: 'SUMIFS for one state and month', runner: 'manual', lang: 'excel',
    prompt: 'Columns: A = State, B = Date, C = Files. Write a formula that totals files for **Wyoming** in **January 2026**.',
    starter: '=SUMIFS(...)',
    solution: '```excel\n=SUMIFS(C:C, A:A, "Wyoming", B:B, ">="&DATE(2026,1,1), B:B, "<="&EOMONTH(DATE(2026,1,1),0))\n```\nThe date criteria are strings concatenated with `&`. `EOMONTH` returns the last day of the month so the formula does not need hard-coded day numbers.' },
  { id: 'xl-xlookup', cat: 'Excel', diff: 'medium', title: 'Two-way lookup in a rate matrix', runner: 'manual', lang: 'excel',
    prompt: 'A rate matrix has policy amounts down column A (A2:A50), zone numbers across row 1 (B1:F1), and rates in B2:F50. Write a formula that returns the rate for the amount in H2 and the zone in I2, with the amount matched to the **next larger** tier.',
    starter: '=XLOOKUP(...)',
    solution: '```excel\n=XLOOKUP(H2, A2:A50, XLOOKUP(I2, B1:F1, B2:F50), "not found", 1)\n```\nThe inner XLOOKUP returns the whole column for the zone; the outer one searches amounts with match_mode 1 (exact or next larger). Alternative: `=INDEX(B2:F50, MATCH(H2, A2:A50, 1), MATCH(I2, B1:F1, 0))` matches the next *smaller* tier — know the difference.' },
  { id: 'xl-dynamic', cat: 'Excel', diff: 'medium', title: 'Unique agents below target', runner: 'manual', lang: 'excel',
    prompt: 'Table `QC` has columns Agent and Accuracy. Write a single dynamic-array formula listing the unique agents whose accuracy is below 98%, sorted A–Z.',
    starter: '=SORT(UNIQUE(FILTER(...)))',
    solution: '```excel\n=SORT(UNIQUE(FILTER(QC[Agent], QC[Accuracy]<0.98, "none")))\n```\nFILTER keeps rows meeting the condition, UNIQUE removes duplicates, SORT orders them. The third FILTER argument avoids a #CALC! error when nobody is below target.' },
  { id: 'xl-let-lambda', cat: 'Excel', diff: 'hard', title: 'Tiered premium with LET', runner: 'manual', lang: 'excel',
    prompt: 'Write a formula for the tiered premium (first 100,000 at 5.75 per 1,000; next 400,000 at 4.50; rest at 3.00) for the amount in A2, using `LET` so each tier is computed once.',
    starter: '=LET(...)',
    solution: '```excel\n=LET(amt, A2,\n     t1, MIN(amt, 100000),\n     t2, MIN(MAX(amt-100000, 0), 400000),\n     t3, MAX(amt-500000, 0),\n     ROUND(t1/1000*5.75 + t2/1000*4.5 + t3/1000*3, 2))\n```\nLET names intermediate values so the sheet stays readable and recalculates faster. Wrap it in `LAMBDA(amt, …)` and define it in Name Manager as `PREMIUM` to reuse it as `=PREMIUM(A2)`.' },
  { id: 'pq-unpivot', cat: 'Excel', diff: 'medium', title: 'Unpivot a wide rate matrix in Power Query', runner: 'manual', lang: 'powerquery',
    prompt: 'A sheet has Amount in column A and one column per Zone (Zone1…Zone5). Describe the Power Query steps (or write the M) that turn it into a tall table with columns Amount, Zone, Rate.',
    starter: 'let\n  Source = Excel.CurrentWorkbook(){[Name="Rates"]}[Content],\n  ...\nin\n  Result',
    solution: '```powerquery\nlet\n  Source = Excel.CurrentWorkbook(){[Name="Rates"]}[Content],\n  Typed = Table.TransformColumnTypes(Source, {{"Amount", type number}}),\n  Unpivoted = Table.UnpivotOtherColumns(Typed, {"Amount"}, "Zone", "Rate"),\n  Result = Table.TransformColumnTypes(Unpivoted, {{"Rate", type number}})\nin\n  Result\n```\nUI path: select the Amount column → Transform → Unpivot Columns → Unpivot Other Columns. "Other columns" keeps the query working when new zone columns are added later.' },
  { id: 'dax-yoy', cat: 'Power BI', diff: 'hard', title: 'Year-over-year files measure', runner: 'manual', lang: 'dax',
    prompt: 'You have a `Production` fact table with `Files` and a marked date table `Dates`. Write DAX measures for Total Files, Files Last Year and YoY % change, handling division by zero.',
    starter: 'Total Files = ...\nFiles LY = ...\nYoY % = ...',
    solution: '```dax\nTotal Files = SUM ( Production[Files] )\n\nFiles LY = CALCULATE ( [Total Files], SAMEPERIODLASTYEAR ( Dates[Date] ) )\n\nYoY % = DIVIDE ( [Total Files] - [Files LY], [Files LY] )\n```\n`SAMEPERIODLASTYEAR` needs a contiguous, marked date table. `DIVIDE` returns BLANK (or an alternate result) instead of an error when last year is zero — an interviewer will ask why you did not write `/`.' },

  /* ===================== DOCUMENT TASKS (self-check) ===================== */
  { id: 'doc-toc', cat: 'Document Automation', diff: 'easy', title: 'Field code for a TOC', runner: 'manual', lang: 'text',
    prompt: 'Write the Word field code that builds a table of contents from Heading 1–3 styles with hyperlinks and page numbers, and say how you force it to update when the document opens (for python-docx generated files).',
    starter: '{ TOC ... }',
    solution: '```text\n{ TOC \\o "1-3" \\h \\z \\u }\n```\n`\\o "1-3"` uses outline levels 1–3, `\\h` makes entries hyperlinks, `\\z` hides tab leaders in Web view, `\\u` uses paragraph outline levels. To force an update on open, set `<w:updateFields w:val="true"/>` in `word/settings.xml` — python-docx can add it via `doc.settings.element.append(OxmlElement("w:updateFields"))` with the `w:val` attribute set to `true`; Word then prompts to update fields.' },
  { id: 'doc-spine', cat: 'Document Automation', diff: 'medium', title: 'Case-wrap cover dimensions', runner: 'manual', lang: 'text',
    prompt: 'A 6 × 9 in hardcover has 320 pages on 50 lb white paper (approx. 0.0025 in per page, PPI 444 for white). Compute the spine width and the full case-wrap flat size including 0.75 in wrap on all sides for a printer that specifies 0.125 in bleed beyond the wrap. Show your working.',
    starter: 'Spine = pages / PPI = ...',
    solution: '```text\nSpine  = 320 / 444          ≈ 0.72 in   (many printers add ~0.06 in for board thickness → 0.78 in)\nWidth  = 0.75 + 6 + 0.72 + 6 + 0.75 = 14.22 in  (+ 2 × 0.125 bleed = 14.47 in)\nHeight = 0.75 + 9 + 0.75 = 10.5 in             (+ 2 × 0.125 bleed = 10.75 in)\n```\nAlways download the printer\'s own template (KDP, IngramSpark, or the print shop) and lay the artwork over it — the wrap and board allowances differ by vendor.' },
  { id: 'doc-acroform', cat: 'Document Automation', diff: 'medium', title: 'Radio group export values', runner: 'manual', lang: 'text',
    prompt: 'A form asks "Policy type" with three options: Owner, Lender, Both. Explain how the three radio buttons must be named and what export values you give them so the data round-trips correctly through FDF, and why a checkbox group would be wrong here.',
    starter: 'Field name: ... Export values: ...',
    solution: 'All three buttons share **one** field name (e.g. `PolicyType`) so the PDF viewer treats them as mutually exclusive; each button gets a distinct **export value** (`Owner`, `Lender`, `Both`). The FDF/XFDF then contains a single `/V /Owner` entry. Three separate checkboxes would allow multiple selections and produce three fields in the export, breaking downstream mapping. In pdf-lib: `form.createRadioGroup("PolicyType")` then `addOptionToPage("Owner", page, {...})` for each option.' },
  { id: 'doc-epub-mimetype', cat: 'Document Automation', diff: 'hard', title: 'Why EPUBCheck rejects your ZIP', runner: 'manual', lang: 'bash',
    prompt: 'EPUBCheck reports "Mimetype file entry is missing or is not the first file in the archive" and "Mimetype file should not be compressed". Write the shell commands that build a valid EPUB from a folder `book/` and explain each.',
    starter: 'cd book\nzip ...',
    solution: '```bash\ncd book\nrm -f ../book.epub\nzip -X0 ../book.epub mimetype          # -0 store (no compression), -X no extra attributes; must be FIRST\nzip -Xr9D ../book.epub META-INF OEBPS   # then everything else, compressed\ncd .. && java -jar epubcheck.jar book.epub\n```\nThe container spec requires `mimetype` to be the first entry, uncompressed, with no extra fields so bytes 30–57 of the file read `mimetypeapplication/epub+zip`. In Python use `zipfile.ZipFile` and write mimetype first with `compress_type=ZIP_STORED`.' },
  { id: 'doc-pipeline', cat: 'Document Automation', diff: 'hard', title: 'Design a JSON → branded DOCX pipeline', runner: 'manual', lang: 'text',
    prompt: 'A client wants 21 branded Word templates generated from one JSON brand file (colours, fonts, logo) and a content JSON per template. Outline the architecture: inputs, steps, libraries, QA checks and how you would make it re-runnable. Then compare it with your own answer.',
    starter: '1. Inputs …\n2. Steps …\n3. QA …',
    solution: '1. **Inputs**: `brand.json` (palette, font names + TTF paths, logo path, footer text), `templates/*.json` (sections, placeholders), a base `.dotx` with styles defined once.\n2. **Steps**: load base template with python-docx → apply brand (theme colours via `styles.xml`/`theme1.xml` edits with lxml, fonts via `w:rFonts`, embed fonts by injecting `fontTable.xml` + `.odttf` or by running Word/LibreOffice post-step) → render sections (headings, tables, images) → insert field codes (TOC, PAGE, DOCPROPERTY) → set core properties → save `.docx`/`.dotx`.\n3. **QA**: open every output with LibreOffice headless to PDF, count pages, check that placeholders like `{{` are gone, validate ZIP parts against OOXML schema, run a visual diff on the PDF against the previous run, EPUBCheck-style report to the client.\n4. **Re-runnable**: pure functions, deterministic output (fixed timestamps in `docProps/core.xml`), version-pinned `requirements.txt`, one CLI command `python build.py --brand brand.json --out dist/`, CI job that rebuilds on every change.' }
];
